# Flask Basic Block

The purpose of this project is to have an easily reproduceable basic Flask starter or "basic block" to build off of for future projects and will include methods and features that are frequently utilized in web based applications.